Oi <%= user.username %>,

Você foi convidado(a) para fazer parte da comunidade <%= group.name %>. Para aceitar o convite, acesse o link abaixo:


Clique no link abaixo para aceitar o convite:

<%= inviteUrl %> 

Se não deseja fazer parte desta comunidade, desconsidere este e-mail.

<%= site.name %>
<%= site.url %>
