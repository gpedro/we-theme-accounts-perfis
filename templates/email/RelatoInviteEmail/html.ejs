Oi <%= user.displayName %>,

<p>Você foi convidado para ser adicionado como <strong><%= modalidade %></strong> no relato <%= relato.titulo %>. Para aceitar o convite, acesse o link abaixo:</p>

<p>
  Clique no link abaixo para aceitar o convite:
  <br>
  <a href="<%= inviteUrl %>" ><%= inviteUrl %></a> 
</p>

<p>Se você não deseja aceitar o convite, desconsidere este e-mail e nenhuma ação será tomada.</p>

<br>

<%= site.name %>
<a href="<%= site.url %>" ><%= site.url %></a> 
