<%- include ../partials/sys-messages.ejs %>

<form action="<%= formAction %>" name="newPasswordForm" class="auth-form user-change-new-form" method="POST">
  <div class="form-content">
    <div class="form-icon">
      <i class="icons-outline icons-outline-padlock-closed"></i>
    </div>
    <h2 class="form-heading"><%= __("user.new-password.title") %></h2>
    <h4><%- __('user.new-password.help') %></h4><br>
       <!-- input: password -->
       <div class="form-group">
        <div class="input-group">
        <input id="newPassword" class="form-control input-lg" required="true" type="password" name="newPassword">
        <label for="password" class="input-group-addon">
          <span class="icons-outline icons-outline-padlock-closed"> </span> <%= __("auth.new-password.form.newPassword") %>
        </label>
       </div>
       </div>
      <!-- /input: newPassword -->
      <div class="form-group">
        <div class="input-group">
          <!-- input: confirm-password -->
          <input id="rNewPassword" class="form-control input-lg" required="true" type="password" name="rNewPassword">
          <label for="confirm-password" class="input-group-addon">
            <span class="icons-outline icons-outline-padlock-closed"> </span> <%= __("auth.new-password.form.confirmPassword") %>
          </label>
          <!-- /input: confirm-password -->
        </div>
      </div>
    <div class="form-group clearfix">
      <div class="input-group pull-right">
        <button class="btn btn-lg btn-success submit" type="submit">
          <%= __("auth.register.form.submit") %>
        </button>
      </div>
    </div>
    <input type="hidden" name="_csrf" value="<%= _csrf %>" />
  </div>
</form>

