<div class="container">
{{partial 'partials/auth-navbar'}}
{{we-sys-messages messages=messages}}
<form role="form" class="auth-form user-signup-form user-login-form"  {{action 'login' on="submit"}}>

  <div class="form-content">

    <div class="user-signup-logo-cdp">
      <img src="/theme/assets/img/signup_cdp.png">
    </div>
    <h2 class="form-heading">{{t "auth.login.title"}}</h2>
    {{!-- <p style="margin: 20px 0;">A CdP migrou para outro servidor, por isso precisa que você recadastre a sua senha. Se você já fez esse procedimento, pode seguir adiante normalmente. Caso ainda não tenha feito clique {{#link-to 'authForgotPassword'}}aqui.{{/link-to}}</p> --}}
    <p style="margin: 20px 0;"></p>

    <!-- input: email -->
    <div class="form-group">
      <div class="input-group" {{bind-attr class="emailErr:has-error:has-success" }}>
        {{input id="email" name="email" value=email  type="email" placeholder="Email" classNames="form-control input-lg" required="true"}}
        <label for="email" class="input-group-addon">
          <span class="icons-outline icons-outline-letter"></span>
        </label>
      </div>
      {{#if emailErr}}
        <div class="input-help error">{{emailErrMessage}}</div>
      {{/if}}
    </div>
    <!-- /input: email -->


    <!-- input: password -->
    <div class="form-group">
      <div class="input-group" {{bind-attr class="passwordErr:has-error:has-success" }}>
        {{input id="password" name="password" value=password  type="password" placeholder="Senha"  required="true" classNames="form-control input-lg"}}

        <label for="password" class="input-group-addon">
          <span class="icons-outline icons-outline-padlock-closed"> </span>
        </label>
      </div>
      {{#if passwordErr}}
        <div class="input-help error">{{passwordErrMessage}}</div>
      {{/if}}
    </div>
    <!-- /input: password -->

    <div class="form-group clearfix">
      <div class="input-group pull-right">
        <button class="btn btn-primary submit" type="submit" {{bind-attr disabled=invalidForm}}>
          {{t "Send"}}
        </button>
      </div>
    </div>

    <div style="text-align: center;">
      {{#link-to "authRegister"}}
        {{t "Sign up"}}
      {{/link-to}}
    </div>

    <input class="eu-gosto-de-mel" type="text" name="mel" value="" style="display: none">

  </div>
  <div class="form-footer">
    <ul>
      <li>
        {{#link-to "authForgotPassword"}}
          {{t "Remember Password"}}
        {{/link-to}}
      </li>
    </ul>
  </div>
</form>
</div>