
<i id="tooltip" data-toggle="tooltip" data-placement="top" title="Migalha de pão" class="breadcrumb-icon fa fa-question-circle fa-lg fa-fw"></i>
{{#each crumb in breadCrumbs}}
  {{#if _view.contentIndex}}
    <li {{bind-attr class="crumb.isCurrent:current:"}}>
      {{#if crumb.linkable}}
        {{#link-to crumb.path}}
          {{crumb.name}}
        {{/link-to}}
      {{else}}
        {{crumb.name}}
      {{/if}}
    </li>
  {{else}}
    <li {{bind-attr class=":first crumb.isCurrent:current:"}}>
      {{#if crumb.linkable}}
        {{#link-to crumb.path}}
          {{crumb.name}}
        {{/link-to}}
      {{else}}
        {{crumb.name}}
      {{/if}}
    </li>
  {{/if}}

{{/each}}
